<h1>
  <%= gravatar_for @user %>
  <%= @user.name %> <%= @user.email %>
  <% if ( current_user?(@user) || (current_user.admin? && !current_user?(@user))) %>
    <%= link_to "delete", @user, method: :delete, confirm: "are you sure you want to delete your account?" %>
  <% end %>
</h1>
